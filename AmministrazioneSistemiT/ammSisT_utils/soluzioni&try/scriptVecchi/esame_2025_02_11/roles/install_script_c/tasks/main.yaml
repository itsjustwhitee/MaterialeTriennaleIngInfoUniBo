---
- name: Copy files
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: /usr/local/bin
    owner: exam
    group: demo
    mode: "750" # rwxr-x--- (lettura ed esecuzione per group)
  loop:
    - "do.sh"
